import { ProfileInfoInput } from '@/widgets/profile'

export const HomePage = () => {
	return (
		<ProfileInfoInput
			className={'class'}
			name={'name'}
			label={'name'}
			placeholder={'Student'}
		/>
	)
}
